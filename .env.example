PUBLIC_SANITY_PROJECT_ID=your_project_id
PUBLIC_SANITY_DATASET=production

# Token de la API de Instagram (cuenta profesional + app de Meta).
# Sin él, el feed usa las fotos locales de public/images/feed/.
# Sin prefijo PUBLIC_: solo se usa en build, nunca llega al navegador.
INSTAGRAM_ACCESS_TOKEN=
